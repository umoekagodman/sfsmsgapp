<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">
    <item android:drawable="@color/splash_background_dark" />

    <!-- Main Logo - 35% from top (vertical bias 0.35) -->
    <item>
        <bitmap
            android:gravity="center_horizontal|top"
            android:src="@drawable/welcome_logo_light"
            android:width="125dp"
            android:height="72dp"
            android:top="35%" />
    </item>

    <!-- Branding Logo - 92% from top (vertical bias 0.92) -->
    <item>
        <bitmap
            android:gravity="center_horizontal|bottom"
            android:src="@drawable/logo_branding_light"
            android:width="97dp"
            android:height="24dp"
            android:bottom="8%" />
    </item>
</layer-list>
